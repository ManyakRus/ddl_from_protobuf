package account

import (
	"github.com/ManyakRus/tinkoff_invest/api/invest_api/investapi"
)

//// ConvertToProtobuf - создаёт модель protobuf из модели crud
//func (m *Account) ConvertToProtobuf() investapi.Account {
//	Otvet := investapi.Account{}
//
	// ConvertFromProtobuf - создаёт модель protobuf из модели crud
	func (m *Account) ConvertFromProtobuf(i ProtoNameMessageName)  {
			m.AccessLevelId = i.access_level_id.AccessLevel
	m.closed_date = i.closed_date
	m.id = i.id
	m.name = i.name
	m.opened_date = i.opened_date
	m.StatusId = i.status_id.AccountStatus
	m.TypeId = i.type_id.AccountType

	return Otvet
}
